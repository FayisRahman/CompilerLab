type

tuple acc {
    int name;
    int b;
    str g;
    acc left;
    acc right;

};

endtype

decl
    int n;
enddecl
int main(){
    decl
        int result;
        acc a,x;
    enddecl
    begin
        read(a.name);
    read(x.name);
    if (a.name == x.name) then
    breakpoint;
     write("They have same names");
    endif;
        return 0;
    end
}
